# showcase-website-

## Render Deployment

This project is set up as a static site for Render.

- Build command: `npm install --prefix frontend && npm run build --prefix frontend`
- Publish directory: `frontend/dist`
- SPA routing: handled by `frontend/public/_redirects`

The Render blueprint is available in [`render.yaml`](./render.yaml).
